resources:
  pipelines:
    rag_pipelines:
      name: Rag Pipelines
      libraries:
        - glob:
            include: ../src/rag_pipelines/transformations/**
      catalog: ${var.catalog}
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src/rag_pipelines
      schema: default
      serverless: true
